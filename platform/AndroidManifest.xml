<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml à copier dans android/app/src/main/AndroidManifest.xml

  Les blocs marqués « audio_service » sont obligatoires pour la lecture en
  arrière-plan : sans le service déclaré avec foregroundServiceType="mediaPlayback",
  la notification disparaît et la lecture s'arrête dès que l'application passe
  en arrière-plan sur Android 14+.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- audio_service : lecture en arrière-plan -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Requis à partir de targetSdk 34 (Android 14) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- just_audio : lecture depuis le CDN -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Soumaya"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!--
          audio_service exige que l'activité principale hérite de
          AudioServiceActivity, ou que MainActivity (Kotlin) étende
          com.ryanheise.audioservice.AudioServiceActivity.
          Voir platform/MainActivity.kt.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- audio_service : le service de lecture -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- audio_service : les boutons du casque et du Bluetooth -->
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
